# ===== Python =====
__pycache__/
*.py[codz]
*$py.class
*.so
*.egg-info/
*.egg
dist/
build/
.eggs/
wheels/
MANIFEST

# Virtual environments
.venv/
.venv_*/
venv/
env/

# ===== Project-specific large files =====
# Model checkpoints (22GB+)
kai0/checkpoints
checkpoints/

# ===== Caches =====
openpi_cache/
kai0/cache
*.tsbuildinfo
.cache/

# ===== Training artifacts =====
logs/
logs
log/
temp/
kai0/wandb/
kai0/dagger_stage_viz
kai0/label_sanity_viz
validation_data/

# ===== Raw data (calibration / sensor capture) =====
calib/data/

# ===== X-VLA self-built datasets =====
# Contents ignored via xvla/data/self_built/.gitignore (folder kept, datasets on-disk only)

# ===== ROS / build outputs =====
install/
ros2_ws/install/
ros2_ws/log/
ros2_ws/build/

# ===== IDE =====
.vscode/
.idea/
**/.idea/

# ===== Secrets / env =====
.env
**/.env
*.env.local
.credentials.json
.volc_creds

# ===== Node =====
node_modules/
**/node_modules/
**/.xla_cache/

# ===== Claude / editor =====
.claude/
*.bak
*.bak.*

# ===== scratch / 本地产物 (eval 探针, 部署包, soup ckpt 等) =====
/_*

# X-VLA env + init (large, vePFS-local)
xvla/X-VLA-env/
xvla/xvla_ckpts/
xvla/ckpts/
xvla/ckpts_official/

# WAM training outputs (large checkpoints)
giga_world_policy/runs/
giga_world_policy/_tmp/
giga_world_policy/scripts/aihc/workspace/

# τ₀-WM 项目: 源码入 git, 仅排除大产物/本地文件
# (checkpoints/ 与 __pycache__/ 已被全局规则忽略)
tau0_wm/.claude/

# NVIDIA/cosmos 工程: 文档/cookbooks/源码入仓; 排除大权重、本地环境、eval 视频与日志
# (.venv/ *.pyc *.bak .claude/ 已由全局规则忽略)
/cosmos/models/
/cosmos/wam_fold_policy/**/*.mp4
/cosmos/wam_fold_policy/**/*.log
/cosmos.oldlink.bak

# tau0_wm training outputs (checkpoints/report assets, 100s of GB)
tau0_wm/runs/
# calibration verify outputs (scratch/outputs, jim-generated on sim01)
calib/verify_out/

# Claude Code local settings (contains image-pull password — never commit)
.claude/settings.local.json
cosmos/wam_fold_policy_runs/
**/__pycache__/
*.pyc

# wam_fold_wm training outputs (checkpoints/reports/exports, 100s of GB)
cosmos/wam_fold_wm_runs/
# fastwam local conda ffmpeg install (vendored libs, ~420M)
fastwam/ffmpeg-libs/

# secrets / external repos / run outputs (auto-added)
.volc_creds
**/.volc_creds
/cosmos/cosmos_policy_pipper_fold_colth/
/cosmos/cosmos_policy_pipper_fold_colth_runs/

# ctrl_wm: vendor code only, exclude weights/env/cache/data (GitHub 100MB/file limit)
/ctrl_wm/.git/
/ctrl_wm/.venv/
/ctrl_wm/.uvcache/
/ctrl_wm/pretrained/
/ctrl_wm/wandb/
/ctrl_wm/logs/
/ctrl_wm/dataset_example/
ctrl_wm/**/*.safetensors
ctrl_wm/**/*.pt
ctrl_wm/**/*.pth
ctrl_wm/**/*.bin
ctrl_wm/**/*.so
ctrl_wm/**/*.h5
ctrl_wm/**/*.msgpack
ctrl_wm/**/*.ckpt
/ctrl_wm/dataset_meta_info_clothfold/
ctrl_wm/**/train_sample.json
ctrl_wm/**/*.mp4
ctrl_wm/**/*.npy
# official X-VLA folding ckpt (HF download, 3.3G model.safetensors) — keep on vePFS, never in git
xvla/official_folding_ckpt/

# ===== VLANEXT (外部 VLA, github.com/DravenALG/VLANeXt) =====
#   VLANEXT 现为独立嵌套 git 仓(自带 .git, origin=github;官方代码 + 我们的 kai0 集成)。
#   父仓整体忽略,避免把嵌套仓当 submodule/embedded repo 误纳入。
#   其内部大产物(data/ckpt/权重)由 VLANEXT/.git/info/exclude 本地忽略。
/VLANEXT/
