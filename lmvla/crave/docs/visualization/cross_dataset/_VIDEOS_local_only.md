# Local-only videos (git-ignored)

> 本目录的 `*.mp4` 已被 `.gitignore` 排除（视频体积大、不可 diff、可重生成），**仅存本地磁盘**，不进仓库。
> 克隆仓库不会带这些视频；需要时在本机重新生成（脚本见 `lmwm/scripts/` 或 `crave/scripts,experiments/` 里对应的可视化脚本）。
> 本清单是它们的轻量占位记录。

| 视频 | 大小 |
|---|---|
| `coffee_transition_video.mp4` | 656K |
| `xvla_transition_video.mp4` | 4.5M |
