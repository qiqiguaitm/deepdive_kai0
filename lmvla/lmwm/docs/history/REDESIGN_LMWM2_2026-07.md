# LMWM-2:milestone 预测为 VLA 赋能 —— 全学习式重设计方案

> 日期 2026-07-08(v2:应用户定案,**弃检索、全学习式**)。定位:从基础思想 + milestone 预测任务的本质特性 + 三路深度文献调研(subgoal/milestone 预测、WM-for-VLA、value/进度表征,2022→2026)出发的重设计。
> **设计红线(用户定案,两条)**:
> **红线一·弃检索**:最终架构不用检索——检索把模型锁死在历史记忆里,跨数据/跨任务能力必须由**学习**获得。检索仅两处降级使用:① 训练期离线挖矿(CRAVE,本来就是);② 实验诊断上界(不随模型交付)。
> **红线二·部署自持(CRAVE-free deployment)**:CRAVE 的簇表、milestone id、价值锚**只存在于训练期 label 工厂**;部署时模型只依赖自身网络输出与在线特征,**零查表、零锚点**(§3e 有逐组件审计表)。
> **红线三·结构入表示,不堆模块**:价值/达成/验证等结构约束不外挂成一堆头和 if-else 门控,而是**塑形进码空间的几何**——价值=方向、达成=距离、验证=模型自身密度。约束即几何,部署即运算。
> 与现 LMWM 的对比与定案见 §5,实验计划见 §6。

---

## 1. 回归基础思想:我们在赌什么,2026 文献怎么裁决

**赌注(2026-07-01 原始规划)**:给 VLA 的提示应是**价值层的 milestone 计划**,而非通用未来帧 —— milestone 是低熵瓶颈态,比固定时距未来帧对行动更有指导性。

### 1a. 文献对赌注本身的裁决:✅ 方向被独立验证,且生态位仍空白

| 证据 | 结论 |
|---|---|
| **VISTA** (2602.10983) | 世界模型只出 **keyframe 级 milestone 序列**(目标图⊕文本子任务),OOD SR 14%→69%;理由与我们同源:"瓶颈态低熵、状态不变性抑幻觉" |
| **DreamVLA** (2507.04447) | 预测**压缩的任务相关抽象**(动态区/深度/语义)> 预测整帧 |
| **LBP** (2505.06861) | 在**冻结 SigLIP 空间**做隐子目标;粗任务对齐子目标 > 密集短时预测;**反向**(从终局倒推)预测误差不随 horizon 累积 |
| **LaWAM** (2606.15768) | 冻结 VFM 特征上的 latent subgoal 条件 VLA = LIBERO 98.6%、24× 快于像素 WM —— 我们的大方向已是 SOTA 配方 |
| **生态位空白** | 所有 latent-WM 竞品(LaWAM τ=1.2s、FLARE、V-JEPA-2-AC、VPP)全是**固定时距**;"以语义 milestone 定义 horizon 的 WM"**无任何已发表系统** —— 这是我们真正的差异化 |

### 1b. 文献对我们**实现方式**的裁决:⚠️ 三张黄牌

1. **无 grounding 的 latent 预测会塌缩**(OpenHelix 2505.03912:辅助 grounding 损失是最大单杠杆 3.45→4.01;HIQL 2307.11949、LCB 2405.04798 同向):隐子目标必须被辅助任务锚住,否则退化成静态指令码。
2. **层级系统的瓶颈常在"接口"而非"生成质量"**(GHIL-Glue 2410.20018):采 K 候选 + 进度分类器**过滤**(负例含时间反序对)+ 当前帧/目标帧**去同步增广**,+25~53% SR。我们把全部力气花在预测侧,接口/验证侧为零。
3. **唯一裁决是下游 SR,我们从未测**。所有 intrinsic 定案(center_w=0.1、teacher=proto、MDN K=4)都可能被 SR 重排。

### 1c. 考古发现的两处内伤(重设计的直接动机)

- **milestone 特有结构被丢掉**:价值单调只当训练标签、milestone 推进无门控、多模态不确定性无出口 —— 现架构退化成"另一个固定形状的 latent WM",与 LaWM 的差异只剩目标标签来源。
- **搁置的裁决**:`archive/next_milestone_vla_validation_plan.md`(2026-07-03)早已写好 GT-first + kill criteria 的决定性验证 plan,**与今天三路文献调研的结论高度吻合,但被搁置从未执行**。

**重设计 = 把 milestone 的价值/事件/不确定性结构以"学习的方式"放回架构 + 把搁置的 SR 裁决放回第一优先级。**

---

## 2. milestone 预测任务的本质特性 → 设计原则(全学习式)

架构必须由任务特性决定,而不是沿用"世界模型=预测未来特征图"的默认形状。

| # | 任务特性 | → 设计原则 | 依据(文献 + 自家实验) |
|---|---|---|---|
| **T1** | **监督空间有界**:milestone 是挖出来的有限集(M≈15–50/任务),但**部署时要外推到没见过的状态与任务** | 有界性放在**训练监督**(簇中心码 teacher)+ **测试时验证器**(学习型 verifier),而不是查表/检索——让网络学"什么样的下一步合法",不是背"哪些下一步出现过" | proto teacher 自家已定案(开放词表);GHIL-Glue 过滤器是**学习出的**关系函数,天然跨任务;检索被否决:锁死历史记忆(用户定案) |
| **T2** | **未来多模态但模式数少**:单帧条件下真实分支 ~2 峰,top1 天花板 ~0.28–0.42(kNN 探针=信息论上限,非容量问题) | **校准的多模态头(MDN K=4 保留)+ 熵门**:不确定时不硬塞提示,把决策权交还策略的语言通道 | 自家容量消融全平(260M deploy +0.00);MDN top3 随 K 单调;GHIL-Glue sample-K-then-verify |
| **T3** | **价值单调是 milestone 的定义性质**(CRAVE 挖矿的构造) | value 必须**上到测试时**:学习型价值头对候选做 value-forward 门 + 进度停滞才重发 —— 而不只是训练标签 | GHIL-Glue(时间反序负例 ≡ 我们的单调性);TaKSIE 2410.11013 / Anticipation-VLA 2605.01772(进度门控重发);V-GPS 2410.13816(value 测试时重排) |
| **T4** | **消费者是 VLA,不是人眼** | payload 必须**近流形 + 空间结构化 + 与策略同塔**;评估只认 **action-MAE/SR**,grid-cos 降级 | LaWAM 消融(空间结构化子目标>pooled);2605.06388(语义空间>重建);OpenHelix grounding;grid-cos 已被自家判"不敏感" |
| **T5** | **身份与外观解耦**:milestone 身份跨 episode 共享,外观属于当前 episode | 身份=预测**连续码**(proto 谱系);外观=**生成器以当前画布渲染**——这恰是"学习"胜过"检索"的自家最硬证据 | forward-from-current oracle 0.935 > absolute 0.82(外观由当前观测带入,学习渲染而非搬历史帧);proto 与 inv 打平但开放词表 |
| **T6** | **horizon 是事件不是时距**:milestone 达成才该推进 | **事件驱动调度**:价值越过锚点→推进 m+2;进度停滞 τ 秒→重发/重选 | 生态位空白(§1a);LBP 反向一致性 |
| **T7** | **跨任务的共享结构在语言与价值,不在像素**:不同任务的 milestone 内容不同,但"指令→阶段推进"的规律共享 | **语言条件化**预测器(现架构是纯状态条件,语言是最大的未用杠杆);价值方向跨任务共享 | π0.5 2504.16054(语言子任务是开放世界泛化来源);VISTA(文本⊕视觉交织);自家 LOO:固定词表在 unseen 任务全弱→出路只能是语言+连续价值流形,不是更大词表 |
| **T8** | **约束应是表示的性质,不是外挂的模块**:价值单调/达成判定/候选验证若各配一个头+门控,部署就成了 if-else 拼盘,且每个头都是新的失效点 | **把结构塑形进码空间几何**:价值=固定方向 u 上的投影、达成=码距、停滞=码速度、验证=预测器自身 MDN 密度 + 编码循环一致 → 部署只剩两个网络 + 纯几何运算 | VIP 2210.00030 / LIV(距离即价值=几何先例);CMD 2406.17098 / TMD(时间距离塑成可组合度量);MDN 自带显式密度(外部 verifier 是给无密度的扩散生成器打的补丁,我们不需要);量纲同一空间→天然零查表(红线二自动满足) |

一句话:**milestone 预测是"带价值序、语言索引、低模式数的下一瓶颈态预测"。跨任务能力来自语言条件 + 价值几何化的连续码空间这两个可学习的共享结构;价值/达成/验证是码空间的几何性质,不是外挂模块,部署自然零 CRAVE。**

---

## 3. LMWM-2 架构(全学习式;价值几何化码空间 = 核心创新)

### 3a. 核心:价值几何化的码空间(结构入表示,T8)

部署组件只有**两个网络 + 一个结构化码空间**。所有"价值/达成/验证/调度"都是码空间上的**几何运算**,不是外挂的头:

```
码空间构造(训练期塑形,部署零查表):
  在线编码:z_t = W · g_t     (W:1152→128 投影,proto 谱系;随 ckpt 交付的小参数,非查表)
  结构损失(训练期,同时塑形 W 与预测器):
    L_proto :MDN 蒸馏下一 milestone 簇中心码(身份监督,已定案的 proto teacher)
    L_mono  :沿 episode 的 ranking loss,使 u·z 随真实时间单调(u = 可学习的"进度方向")
              — 相对序监督而非绝对回归:规避自家 AE 塌缩老坑(绝对 progress 回归塌平台)
  于是码空间自带四个几何性质(全部部署可用、全部零 CRAVE):
    进度   v_t        = u · z_t                       (价值=方向上的投影,无价值头)
    前向门 forward(ẑ_k) = u · (ẑ_k − z_t) > δ          (价值单调=前向锥,无查表锚点)
    达成   reached     = ‖z_t − ẑ*‖ < τ_r             (码距,无 reach 头)
    停滞   stalled     = 窗口 τ_s 内 |u·Δz_t| ≈ 0      (码速度,自参照)
```

### 3b. 数据流

```
在线(全部在 π0.5 SigLIP 同塔空间,冻结 ~400M;部署无 bank、无索引、无锚点):
  帧 224² → SigLIP grid G_t (16×16×1152) + gist g_t (1152) → z_t = W·g_t (128)
  语言 l → π0.5 原生语言嵌入(取 prefix 语言槽 pooled 向量,冻结)

  ① 预测器(~3.5M,MDN K=4,继承 predm 谱系 + 条件化):
       p(z_next | g_t ⊕ lang ⊕ proprio ⊕ prev_ẑ)  → 候选码 {ẑ_k, π_k}
       — teacher = proto(簇中心码,已定案):连续码=身份=开放词表
       — 新增条件:lang(T7 跨任务主杠杆)、prev_ẑ / proprio / t_bin(探针 +11/+5.2/+3.5pt 从未落地;prev_ẑ 是自己上一次的输出,自产)
  ② 候选选择(零参数,纯几何+模型自身密度):
       score(ẑ_k) = π_k(MDN 自身后验:低密度=off-manifold,模型自带 verifier,
                     外部 verifier 是给无密度的扩散生成器打的补丁,MDN 不需要)
                  × 𝟙[forward(ẑ_k)](前向锥门)
       全否 / 熵高 → 熵门:本步不注入,交还 π0.5 语言通道
  ③ 生成器(30M AdaLN,原样保留):Ĝ_sub = AdaLN(G_t 画布, ẑ*)
       — 学习渲染,外观由当前观测带入(T5;0.935>0.82):对 unseen 场景零历史依赖
       — 循环一致自检(零参数,仅测试时):‖W·pool(Ĝ_sub) − ẑ*‖ 大 → 渲染失败,弃用本候选
         ⚠️ 只作测试时检查,严禁作训练目标(dec_reencode_v2 刷分教训:训上去必被游戏)
  ④ 注入 π0.5(复用 archive/lmwam_v2 接线):
       Ĝ_sub(可 4× 下采样 → 64 token)进 prefix 新视觉槽
       + type-embedding + 零初始化投影 + KI stop-grad + 去同步增广 + subgoal-dropout
       对照臂 = FLARE 式(2505.15659):无显式 token,只加"未来 token 对齐 milestone 嵌入"辅助损失

调度(事件驱动,T6;全几何,零 CRAVE):
  reached(‖z_t − ẑ*‖<τ_r)→ ① 以 prev_ẑ=ẑ* 重预测下一 milestone(prev_ẑ 条件天然滞回防抖)
  stalled(u·Δz_t≈0 超 τ_s)→ ② 重选候选重发(次优模式上位)
  熵门 → 不注入,退语言通道

离线(label 工厂,只在训练期存在,不随模型交付):
  CRAVE 挖矿(per-task)+ UVD(2310.08581)反向单调性交叉验收
  + bank 质量验收三关卡(簇内纯度 / value 跨度 / 身份可分度;治 vis id3=0.11 无验收上桌的教训)
```

### 3c. 部署形态

| | LMWM(现) | LMWM-2 |
|---|---|---|
| 在线网络 | 2 个(predm 3.3M + 生成器 30.3M) | **仍 2 个**(① 3.5M + ③ 30M)+ W/u 小参数(~0.15M,随 ckpt) |
| 部署依赖 | 无索引 ✓ | 无索引、无锚点、无 CRAVE ✓(§3e 审计) |
| 语言条件 | ✗(纯状态) | ✓ |
| 测试时行为 | 单次前向盲出 | 候选→几何门→事件调度,且有"不出手"选项——**新增的全是运算,不是网络** |
| 注入 π0.5 | 未接 | 设计核心(双臂)|

### 3d. 部署自持审计表(红线二逐组件核查)

| 组件/运算 | 部署输入 | CRAVE 依赖(部署时) |
|---|---|---|
| ⓪ SigLIP 编码 | 帧、指令 | 无 |
| ① 预测器 | g_t⊕lang⊕proprio⊕prev_ẑ | **无**(proto teacher 只在训练期;prev_ẑ 是自己上一步输出) |
| ② 候选选择 | π_k、u·(ẑ_k−z_t) | **无**(MDN 自身后验 + 前向锥;u 是学到的方向参数,非价值锚查表) |
| ③ 生成器 + 循环自检 | G_t、ẑ*、W | 无 |
| 调度(达成/停滞) | ‖z_t−ẑ*‖、u·Δz_t | **无**(码距/码速度,自参照;旧稿"v 越过 m* 锚点"需查 CRAVE 价值锚——已废除) |
| 常量 | W、u、gmu/gsd 归一化统计 | 以模型常量随 ckpt 交付(类 BN 统计),非查表 |
| 离线挖矿/验收 | — | 仅训练期(标签工厂) |

### 3e. 跨任务能力从哪里来(学习式回答)

1. **语言条件**(T7):任务身份由指令携带,预测器学"指令+状态→下一阶段"的映射,新任务=新指令,无需新词表;
2. **价值几何化码空间**(T8):身份活在连续流形(LOO 已证连续锚 unseen 小胜),进度方向 u 跨任务共享——"快到下一个瓶颈了"是与任务内容无关的几何信号;
3. **MDN 自身密度**:多任务联训后,密度低=没见过的组合,自动触发熵门退语言通道——失败模式是"不出手",不是"胡出手";
4. **生成器画布机制**(③):外观从当前观测来,对 unseen 场景零历史依赖。

### 3f. 降级阶梯(❗已被 §3f-实测更新为**修正后定档**,不再是预案而是结论)

| 机制 | 原优雅方案 | 实测 | **定档(2026-07-08)** |
|---|---|---|---|
| 价值/进度 | u·z 码上线性方向 | ❌ 0.557<gist | **退独立价值头**(但单帧仅 0.648,弱)→ 事件调度**主用身份切换**、价值降为软辅助 |
| 验证/弃权 | MDN pi 熵门 | ❌ ECE 0.637,pi 塌陷 | **改用 MDN 密度弃权**(密度分对错 AUC 0.86;正在测密度-correctness 分离度) |
| 候选可信 | MDN 密度打分 | ✅ AUC 0.857/0.996 | **保留**(优雅胜,无需外挂 verifier) |
| 达成 | 码距阈值 | ⚠️ AUC 0.575 | **测身份切换 vs 学习头**(reach_idchange / reach_head_auc 0.651)择优,二选一 |
| 身份 | proto 码空间 MDN | ✅ 0.7564/0.7098 复现 | **保留** |

—— 所有替换仍满足红线一/二(学习式、零 CRAVE、零查表);**退的是"单一码空间统一身份+价值"的 T8 优雅,不退部署自持**。部署仍 2 网络;价值头(~0.2M)+ 可能的 reach 头(~0.1M)是被数据要求加回的,不是堆的。

### 3f-实测. P1/P2 首轮实测判决(2026-07-08,本地 3 任务 kai0+coffee+xvla,12k step)

> 脚本 `lmwm/scripts/train_lmwm2.py`;两臂 base(无条件)/ cond(task-emb+prev_ẑ)。CRAVE 仅作考卷。**这是对 §3a 每个几何机制的第一次数据裁决,结论重要且部分证伪 T8 优雅假设。**

| 机制(§3a) | 探针 | 数值 | 判决 |
|---|---|---|---|
| **价值=方向 u·z**(T8 核心) | 时间序 mono:u·z码 / 线性gist / MLP gist | **0.557 / 0.612 / 0.648** | ❌ **证伪**:价值方向在 128 维 proto 码空间里比裸 gist 更弱;u 与 CRAVE 值相关仅 0.18–0.26。身份和价值想要不同表示,单码空间承载不了两者 |
| 单帧进度上限 | MLP-gist mono | 0.648 | ⚠️ 即便最好也弱——**单帧进度有信息论天花板**(与 top1 身份 0.28–0.42 同源),价值需时序上下文或降为软信号 |
| **验证=MDN 自身密度**(优雅) | verify AUC(倒退/错任务负例) | **0.857 / 0.996**(cond 0.877/0.998) | ✅ **成立**:MDN 显式后验自带 verifier,仅微弱输给外挂 head(0.890/0.9997)→ **不需要外挂 verifier 网络** |
| **熵门放弃权**(不确定→退语言) | entropy_mean / ECE / gate@0.9 | 0.0003 / **0.637** / **0** | ❌ **证伪**:MDN pi 塌成 one-hot,永远满自信却仅 36% 对 → **熵门形同虚设**;不确定性信号要改用**密度**(密度能分对错 AUC 0.86,pi 熵不能) |
| 达成=码距 | reach:码距 AUC / 学习头 AUC | 0.575 / **0.651** | ⚠️ 码距弱,学习头更好;正在测"身份切换"作为第三方案(§3f 预案候选) |
| 身份码空间 | deploy/id3(vs proto 0.753/0.710) | **0.7564/0.7098** | ✅ 忠实复现,基座可信;码空间是好的**身份**空间 |
| 条件化(lang 代理 + prev_ẑ) | deploy / id3 / xvla-id3 / code-top1 | +0.002 / +0.004 / **+0.018** / **+0.027** | ✅ 小而一致的正增量,最难的 xvla 身份与 code-top1 提升最明显;拆解 task vs prevz 归因中 |

**一句话判决**:**"完全统一的价值几何"优雅性(T8 核心)被数据证伪——身份几何成立、价值几何不成立。** 但架构不塌:优雅的部分(身份码空间 + MDN 密度 verify)保留,失效的部分(u·z 价值方向、熵门)按下方修正后的降级阶梯替换。这正面回答了"优雅 vs 应急"之问:**优雅是一个可证伪的假设,这次一半成立一半被证伪,以数据为准而非以美感为准。**

### 3g. 明确不采纳的文献建议(自家证据否决,防重复踩坑)

- **"预测器换 ViT"**:自家 H7 已证空间 token 对身份预测无增益(grid vs pooled A≈B)、gist vs grid 打平(Δ0.0003)。ViT 只在"网络必须输出 grid"时有意义——③ 已由 AdaLN CNN 胜任且有消融定档(concat 0.52 / nolift 0.57 vs 定档 0.99)。
- **"上 diffusion/flow 子目标头"**:自家实测 flow best-of-16 0.834 < 回归 0.872,MHP/MCL deploy 反降。多模态上限来自单帧条件的信息论天花板;top-K+verifier 是正确出口。
- **"换 V-JEPA/DINO 目标空间"**(2605.06388):同塔 SigLIP 的 FLARE 逻辑 + 自家 E2 实测(0.716>0.694)优先;留 LaDi-WM 式双空间辅助损失作远期消融,不换塔。
- **"检索真实帧做 payload"**(GHIL-Glue/UVD/B2FF 一系):用户定案否决——锁死历史记忆,跨任务要靠学习。其 on-manifold 优点由 ③画布渲染 + 循环一致自检补偿;其"过滤"精髓由 MDN 自身密度 + 前向锥以几何形式继承(零新增网络)。
- **"外挂 GHIL-Glue 式 verifier 网络"**:对无密度的扩散生成器它是必需补丁;我们的 MDN 有显式后验,先用模型自身密度(T8),外挂 verifier 降为预案 2(§3f)。

### 3h. 编码器耦合 + DINO 沙箱(实测判决,2026-07-08)

**问题**:LMWM 在线吃 VLA 的 SigLIP,但 VLA 后训练时塔会漂;且 DINO 空间(印象中)解码更锐。两个衍生设计问:①要不要"SigLIP→DINO→预测/解码→DINO→SigLIP"的双适配头沙箱?②漂移用联合后训练还是适配头?

**沙箱实测否决**(`probe_siglip2dino_decode.py`,kai0 3000帧/8000步,patch-grid 解码器):

| 解码路径 | L1 vs 原图 ↓ | 锐利度 ↑ |
|---|---|---|
| DINO-true(天花板) | 0.0247 | 209.6 |
| **SigLIP-native** | **0.0268** | **217.2** |
| 沙箱 SigLIP→适配→DINO | 0.0414 | 151.7 |
| 适配器 feat-cos(SigLIP→DINO) | **0.735**(充分训练仍到顶) | |

→ **两条结论**:(a)**"DINO 解码远优于 SigLIP"的前提在 grid 解码器下不成立**——SigLIP-native(0.0268/217)≈DINO-true(0.0247/210)、锐度还略高;旧印象来自 pooled 解码器时代(pooled DINO L1 0.062)。(b)**沙箱最差**:输入适配 SigLIP→DINO 是"穷→富",cos 充分训练也只到 0.735(SigLIP 确实没编码 DINO 的全部信息),损失直传解码 → L1 +54%、更糊。**双适配头沙箱花两个适配 + 840M DINO 离线负担,换来更差的解码 → 否决。** 反而强化 LMWM 主论点:VLA 自己的 SigLIP 塔外观已够,LaWM 的额外 DINO 塔是不必要负担。→ **定档 Path α:milestone 在 SigLIP 原空间预测+解码,DINO 只留离线 CRAVE 挖矿,零在线适配。**

**塔漂移耦合定档**:两选项不互斥,最优是合流——
1. **先查前提**:目标 π0.5 微调是否真动 SigLIP 塔?π0/π0.5 常冻结视觉塔;若冻结,漂移问题不存在,直接复用(编码器非强制依赖的最干净兑现)。
2. 若动:**WM 核心 KI 冻死**(保护 §3f-实测 证实脆弱的价值/身份结构不被动作梯度覆盖),**塔依赖全塞进一个薄适配头**(VLA 当前塔→LMWM 规范空间);漂移时只自监督重拟合薄壳(便宜、无 SR 标签、无集群)。
3. **"联合"降级为只训接口**:薄适配头 + 注入投影可在 VLA 后训练时随动作目标一起训,核心永远冻结——与 SOTA 一致(FLARE/InternVLA-A1.5 都是冻 target、只联合策略侧消费)。纯全量联合仅留作 SR 打不出增量的最后逃生舱。
4. **诚实边界**:适配头只能重投影两塔共享信息,救不回塔丢掉的信息;检测法=漂移后经适配头算 LMWM intrinsic 指标,掉了就说明塔遗忘,再升级重蒸馏预测器输入层。

---

## 4. 三路调研浓缩(20 篇核心证据索引)

| 主题 | 论文(arXiv) | 对 LMWM-2 的贡献 |
|---|---|---|
| 接口是瓶颈/候选过滤 | GHIL-Glue 2410.20018 | 过滤思想(时间反序≡单调性)以 MDN 密度+前向锥几何继承;去同步增广 |
| 价值=几何 | VIP 2210.00030 / LIV 2306.00958 / CMD 2406.17098 | u·z 进度方向、码距达成——距离/方向即价值的文献先例(T8) |
| milestone 序列 WM | VISTA 2602.10983 | keyframe 级预测抑幻觉、文本⊕视觉交织 |
| 冻结空间隐子目标 | LBP 2505.06861 | SigLIP 空间验证、粗>密、反向一致 |
| 零成本对照臂 | FLARE 2505.15659 | 辅助对齐损失,无推理开销,+26% |
| 隐子目标要 grounding | OpenHelix 2505.03912 / HIQL 2307.11949 / LCB 2405.04798 | L_mono 即价值 grounding,防码空间塌缩(最大单杠杆) |
| 进度门控重发 | TaKSIE 2410.11013 / Anticipation-VLA 2605.01772 | 事件驱动调度 |
| value 测试时用 | V-GPS 2410.13816 / GVL 2411.04549 | 前向锥门;GVL=CRAVE 的零训 baseline |
| 语言=跨任务索引 | π0.5 2504.16054 / Hi Robot 2502.19417 | ① 语言条件化(T7) |
| 任务相关抽象>整帧 | DreamVLA 2507.04447 | 赌注验证;分块注意力保护 action token |
| 注入方式 | DUST 2510.27607 / LaWAM 2606.15768 | 空间化>pooled;KI/零初始化;latent-action 蒸馏可选 |
| 挖矿对标 | UVD 2310.08581 / InfoCon 2404.10606 / SARM 2509.25358 | 边界交叉验收 / 信息量筛选 / ② 阶段-回归结构 |
| 视觉 CoT / 子目标图 | CoT-VLA 2503.22020 / SuSIE 2310.10639 / Seer 2412.15109 | 长程 +10~17%;GT goal-image OOD +40% |

---

## 5. LMWM-2 vs LMWM 现架构:逐维对比与定案

### 5a. 逐维对比

| 维度 | LMWM(现) | LMWM-2 | 谁优 / 证据 |
|---|---|---|---|
| 子目标身份 | MDN K=4 连续码(proto),**纯状态条件** | 同谱系 + **语言/prev_ẑ/proprio 条件** | **LMWM-2**:语言是 T7 主杠杆;探针 +11/+5.2/+3.5pt 从未落地 |
| 子目标外观 payload | 生成(AdaLN 画布渲染) | **保留同款** + 循环一致自检(零参数) | 平/略优;画布渲染是现架构最有价值的资产(0.935>0.82),学习式跨场景的根 |
| off-manifold 风险 | 有,靠"仅可视化"豁免 | MDN 密度门 + 前向锥 + 循环自检 + 熵门,四层全零参数 | **LMWM-2**(诚实:仍非零,但每层都不是新失效点) |
| value 的用法 | 仅训练标签 | **塑形进码空间几何**(u 方向),测试时=前向锥门/停滞检测 | **LMWM-2**;兑现"价值层 WM"承诺且零查表零新头 |
| horizon | milestone(名义),推进无门控 | 事件驱动:码距达成 + 码速度停滞(纯几何) | **LMWM-2**(论文生态位主张;部署零 CRAVE) |
| 多模态出口 | MDN 有分布但部署只取均值 | 候选 × 密度 × 前向锥 → 熵门放弃权 | **LMWM-2**:不确定性有了行为学出口,且用的是模型自带的分布 |
| VLA 注入 | 未接(半成品) | 设计核心,双臂 + kill criteria | **LMWM-2** |
| 跨任务机制 | 词表并集 / proto 码 | 语言条件 + 价值几何化连续码空间 | **LMWM-2**;LOO 已证固定词表在 unseen 全弱 |
| 部署形态 | 2 网络单前向 | **仍 2 网络**(34M)+ 纯几何运算 | 平;新增全是运算不是网络(T8);K≤4 次生成器前向 ≪ 一次 π0.5 前向 |
| 已验证程度 | intrinsic 充分(reach 1.67>LaWM 1.48) | ①③继承已验证;码空间塑形/几何调度/注入未跑 | **LMWM**;故 P0/P1 先裁决后建设 + §3f 三级降级预案 |

### 5b. 继承关系(LMWM-2 = 现架构的结构升级,不是推倒)

- ①预测器 = predm + proto teacher **原样继承**,加条件化(lang/prev_ẑ/proprio);
- ③生成器 = **原样继承**(30M AdaLN,消融已定档);
- 码空间 = proto 的固定投影 W **升级**为"身份(L_proto)+ 进度方向(L_mono ranking)"双约束塑形——AE 塌缩教训(绝对回归塌平台)决定了用相对序不用绝对值;
- GHIL-Glue 过滤精髓 → MDN 自身密度 + 前向锥(几何继承,零新增网络;外挂 verifier 降为预案);
- 注入 = `archive/lmwam_v2_plan_20260704.md` 接线方案直接复用(KI/零初始化/token 预算已想清楚);
- Phase 0 = `archive/next_milestone_vla_validation_plan.md` 的 GT-first kill criteria 复活升级。

### 5c. 定案

**采纳 LMWM-2(全学习式)为最终方案。现架构的短板不是"预测器/生成器本身",而是五个缺失的学习结构:语言条件、测试时价值、事件调度、不确定性出口、VLA 注入。裁决权交给 P0 的 oracle-SR:**
- 若 A1(oracle 码→生成 payload)>A0:主链路成立,进 P2 换真预测器;
- 若 A1≈A0 但 A_diag(oracle 检索真实帧,**仅诊断**)>A0:说明生成保真是短板 → 火力集中 ③(生成质量)与循环自检阈值,而不是放弃命题;
- 若全部 ≈A0:子目标条件对 π0.5+语言无正增量 → 诚实收口,转 FLARE-aux 路线(不要求"注入有用",只要求"预测未来是有用的训练信号");
- 无论哪支,**milestone-horizon vs 固定 1.2s** 消融保留——这是对基础思想的直接检验,也是相对 LaWAM 的论文主张。

---

## 6. 实验计划(2026-07-08 用户调序:**P1/P2 本地并行先行,P0 oracle/VLA 注入后延**)

> 执行顺序 = P1 ∥ P2(本地 2 卡,纯 intrinsic,不碰 π0.5)→ P0(oracle 裁决,后延至 P1/P2 收敛后)→ P3。
> P2 原判据依赖 A*>A0,后延后 P2 改为**纯 intrinsic 消融**:几何 vs 模块每对都在回放评测上裁决;涉及 SR 的最终裁决保留给后延的 P0。

### P0 —— oracle 裁决(**后延**;最高信息量,不训任何新 WM)
| 臂 | 内容 | 角色 |
|---|---|---|
| A0 | π0.5 baseline(语言条件,无子目标) | 基线 |
| A1 | + **oracle 码 → 生成器 payload**(GT next-milestone 码,现 ③ 渲染) | **主臂**:学习式 payload 上界 |
| A2 | + oracle milestone **文本标签** | 最便宜通道 |
| A_diag | + oracle **检索真实帧** grid | **仅诊断**(度量生成保真损失多少增量;不进架构、不交付) |

- 训法:LoRA(gemma_300m_lora r32)+ KI 全冻结 provider + 零初始化投影 + 去同步增广 + subgoal-dropout;单 top-head 视图先行。
- 评估阶梯:**kai0 离线 action-MAE**(天级先出信号;记 v3-PTS 教训:离线 MAE 只作排序不作绝对判断)→ **LIBERO-Long 闭环 SR**(sim 决定性;`fastwam/experiments/libero/` 已有 π0 闭环,需 CRAVE 套 LIBERO demo)→ 真机叠衣(域)。
- **kill criteria**:全臂 ≈A0 → 停注入路线转 FLARE-aux;A1>A0 → P2;A1≈A0<A_diag → 火力集中生成质量(③④⑤)。
- 算力:LoRA 微调走集群(H20 队列/gf3 恢复后);本地 2 卡做接线 smoke + 离线 eval。

### P1 —— 码空间塑形 + 几何自持性(本地 2 卡,**立即执行**)
- **码空间塑形**(核心):L_proto + L_mono(episode 内 ranking)训 u(先固定 W 作最纯的线性检验=T8 哨兵;学 W 作第二臂)(train_multitask.py 骨架);
- **几何自持性离线回放评测**(验红线二):在 val episode 上回放几何调度器,报告四件套——u·z 单调率、码距达成判定 F1、前向锥门通过率、熵门触发率(CRAVE 值/边界**仅作考卷**,不作零件);
- ① 条件化改造(task-emb[语言代理,单指令数据集下等价]/prev_ẑ/proprio)、循环一致自检、bank 验收三关卡。
产出:`LMWM2Provider`(纯前向、全冻结、零索引、零锚点)。

### P2 —— 几何 vs 模块 intrinsic 消融(本地 2 卡,**与 P1 并行**;原"换真预测器"的 SR 部分并入后延的 P0)
每条对应一个设计赌注,判据 = P1 回放评测四件套 + top-K acc:
- u·z 线性方向 vs 独立价值头 MLP(§3f 预案 1):单调率/停滞检测质量;
- MDN 密度校准 vs 外挂 verifier(预案 2):π_k 置信-正确率校准曲线、熵门扫描;
- 码距达成 vs 学习 reach 头(预案 3):达成 F1;
- 条件化增量:baseline(纯 gist)vs +task-emb vs +prev_ẑ vs +proprio(探针 +11/+5.2pt 兑现检验);
- top-K 几何选择 vs top1 硬目标。
判据:几何版不输模块版(输了按 §3f 逐级降级,诚实记录);预测误差对 oracle 的差距量化留给 P0。

### P3 —— 论文主张与 scaling
milestone-horizon vs 固定 1.2s 消融(生态位);GVL 零训 value baseline;UVD 对标;跨任务(3 任务联合)+ LOO 开放词表复测——这次以 action-MAE 为准,并验证语言条件是否兑现 unseen 任务泛化。

### 指标纪律
主指标 = action-MAE → 闭环 SR;grid-cos 降级 sanity;新增(几何自持性四件套):u·z 单调率、码距达成 F1、前向锥通过率、熵门触发率(P1 回放评测,CRAVE 仅作考卷)+ milestone top-K acc + bank 验收三关卡。

---

## 7. 风险与止损

| 风险 | 概率 | 止损 |
|---|---|---|
| 子目标条件对 π0.5+语言无正增量(H0) | 中(短任务大概率,长程文献支持有) | 只在长程测;kill criteria 明确;FLARE-aux 是有尊严的退路 |
| 生成 payload off-manifold 伤 VLA(弃检索的代价) | 中 | 密度门+前向锥+循环自检+熵门四层压制;A_diag 量化代价;若 A_diag≫A1 火力集中生成质量 |
| **线性方向 u 装不下弯曲的价值流形**(T8 的最大赌注) | 中 | L_mono 是相对序(ranking)不是绝对回归,弯曲流形上仍可单调化;P1 回放评测单调率是早期哨兵;不行退 §3f 预案 1(独立价值头,丢优雅不丢红线) |
| MDN 密度作验证不可靠(校准差/过自信) | 中 | 密度只做门不做排序主力(排序靠前向锥);P1 报告密度-正确性校准曲线;不行退预案 2(外挂 verifier) |
| 码距达成判定抖动/阈值敏感 | 中 | prev_ẑ 滞回 + 双阈值;P1 回放 F1 早期哨兵;不行退预案 3 |
| 循环一致自检被游戏 | 低(不训只测) | 铁律:严禁进训练目标(dec_reencode_v2 教训);仅测试时弃用判据 |
| 语言条件在单指令数据集上学不出区分度 | 中(kai0/coffee 每任务指令单一) | 跨任务联训天然提供语言方差;P3 LOO 是真检验;不行则退 prev_ẑ/proprio 条件仍有探针背书 |
| bank 质量不过关(vis 教训) | 中 | 验收三关卡前置,不过关任务不进联训 |
| LIBERO 需重挖 bank,周期长 | 高 | 离线 action-MAE 先行;LIBERO 并行准备 |
| 集群/gf3 不可用 | 现状 | P1 本地先行;P0 微调排 H20 队列 |

---

## 8. 参考

三路调研原文(subgoal 预测 / WM-for-VLA / value-进度表征)由 2026-07-08 深度调研产出,核心 20 篇见 §4 表。自家证据全部来自 `PITFALLS_AND_HISTORY.md` 与 `ABLATION_CONVERGENCE_2026-07.md` 的实测编号。
前置文档:`ARCHITECTURE_AND_BASELINE.md`(现架构)、`archive/lmwam_v2_plan_20260704.md`(注入接线)、`archive/next_milestone_vla_validation_plan.md`(GT-first 验证原案)。
版本注:v1(RAMP,检索锚定)因用户定案"最终架构不用检索、跨任务能力必须学习获得"而废弃;其 verifier/门控/事件调度精髓以学习式保留于本方案。
