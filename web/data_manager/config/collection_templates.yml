templates:
- id: task_a_base
  task_id: Task_A
  subset: base
  prompt: "Flatten and fold the cloth."
  enabled: true
  note: "Task A base demo — prompt must match training config pi05_flatten_fold_normal"
- id: task_a_dagger
  task_id: Task_A
  subset: dagger
  prompt: "Flatten and fold the cloth."
  enabled: true
  note: "Task A DAgger correction — same prompt as base"
- id: task_b_base
  task_id: Task_B
  subset: base
  prompt: "Fetch the clothes, fold the tee shirts and hand-over the collared shirts."
  enabled: true
  note: "Task B TeeShirtSort base"
- id: task_b_dagger
  task_id: Task_B
  subset: dagger
  prompt: "Fetch the clothes, fold the tee shirts and hand-over the collared shirts."
  enabled: true
  note: "Task B TeeShirtSort DAgger"
- id: task_c_base
  task_id: Task_C
  subset: base
  prompt: "Fetch and hang the cloth."
  enabled: true
  note: "Task C HangCloth base"
- id: task_c_dagger
  task_id: Task_C
  subset: dagger
  prompt: "Fetch and hang the cloth."
  enabled: true
  note: "Task C HangCloth DAgger"
